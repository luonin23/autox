{
  "name": "fold7-agent",
  "version": "1.0.0",
  "description": "用本地大模型（或 Kimi API）操控 Android 手机，实现语音/文字指令自动化",
  "scripts": {
    "test": "cd autojs-scripts && node test/runner.js",
    "mock": "cd autojs-scripts && node -r ./test/mock.js main.js"
  },
  "keywords": ["autojs", "automation", "android", "llm", "kimi", "accessibility"],
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
